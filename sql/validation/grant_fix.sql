# Supplemental Logging & ARCHIVELOG Verification

Checks that:
- Database is in ARCHIVELOG mode
- Supplemental logging is enabled
- Required log archive destinations are configured

Both are prerequisites for Oracle CDC via LogMiner.
