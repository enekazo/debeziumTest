<?xml version="1.0" encoding="UTF-8"?>
<!--
  Marks this JAR as a CDI bean archive so Quarkus (Debezium Server) will
  scan it for @Named / @Dependent beans such as FabricMirroringSink.
  bean-discovery-mode="all" is required because the classes are not
  annotated with bean-defining scope annotations visible at the archive level.
-->
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                           https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       version="4.0"
       bean-discovery-mode="all">
</beans>
